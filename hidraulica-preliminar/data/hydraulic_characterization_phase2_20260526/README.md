# Hydraulic characterization phase 1

Este export liviano resume corridas `unblocked` y `with_block_ref`.

- `unblocked`: flujo incidente transferible para la ecuacion final.
- `with_block_ref`: diagnostico de perturbacion/interaccion con bloque estable.

No incluye `.bi4`, `.ibi4`, VTK, `Part*`, `cases/` ni `_out`.
